[package]
name = "kasane"
version = "0.0.2"
edition = "2021"

[dependencies]
kasane-logic = { git = "https://github.com/AirBee-Project/Kasane-Logic.git" }
redb = { version = "3.1.0", optional = true }

[features]
in_memory = []
on_disk = ["dep:redb"]
info:
  name: revoke_global
  type: grpc
  seq: 5
  tags:
    - Users

grpc:
  url: "{{baseUrl}}"
  method: /kasane.UserService/RevokePrivilege
  methodType: unary
  metadata:
    - name: authorization
      value: "Bearer {{accessToken}}"
  message: |-
    {
      "username": "alice",
      "target": {
        "global": {}
      }
    }

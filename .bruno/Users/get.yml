info:
  name: get
  type: grpc
  seq: 2
  tags:
    - Users

grpc:
  url: "{{baseUrl}}"
  method: /kasane.UserService/Get
  methodType: unary
  metadata:
    - name: authorization
      value: "Bearer {{accessToken}}"
  message: |-
    {
      "username": "root"
    }

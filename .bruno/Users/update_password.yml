info:
  name: update_password
  type: grpc
  seq: 4
  tags:
    - Users

grpc:
  url: "{{baseUrl}}"
  method: /kasane.UserService/UpdatePassword
  methodType: unary
  metadata:
    - name: authorization
      value: "Bearer {{accessToken}}"
  message: |-
    {
      "username": "alice",
      "password": "newsecret123"
    }

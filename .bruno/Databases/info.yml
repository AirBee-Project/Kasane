info:
  name: info
  type: http
  seq: 2
  tags:
    - databases

http:
  method: GET
  url: "{{baseUrl}}/databases/:db_name"
  params:
    - name: db_name
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Get database info
    request:
      url: "{{baseUrl}}/databases/:db_name"
      method: GET
      params:
        - name: db_name
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "name": ""
          }

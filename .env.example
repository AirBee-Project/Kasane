# ==========================================
# 基本設定
# ==========================================

# 保存先ディレクトリ（未指定時は `default_kasane_db`）
DATABASE_DIR=default

# APIサーバーのポート
PORT=5173

# ログの出力レベル
LOG_MODE=kasane=info,tower_http=info

# ==========================================
# セキュリティ・認証
# ==========================================

# 初回起動時に作成される root ユーザーの初期パスワード
ROOT_PASSWORD=password

# JWTアクセストークンの署名に使用する秘密鍵（本番環境では必ずランダムな文字列に変更）
JWT_SECRET=kasane-super-secret-key-change-me

# ==========================================
# データベースエンジンの高度なチューニング
# ※基本的に触るな
# ==========================================

# LMDBが確保する仮想メモリマップの最大サイズ（デフォルト: 10GB）
# KASANE_LMDB_MAP_SIZE=10737418240

# ディスク先読みの無効化（巨大データ/ランダムアクセス時に 1 を設定してI/Oを節約）
# KASANE_LMDB_NO_READAHEAD=1

# 書き込み可能なmmapの有効化（危険かも）
# KASANE_LMDB_WRITE_MAP=1

# コミット毎のfsyncの省略（危険かも）
# KASANE_LMDB_NO_SYNC=1